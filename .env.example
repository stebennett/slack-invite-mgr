# Google Sheets Integration
GOOGLE_CREDENTIALS_FILE=path/to/your/service-account.json
GOOGLE_SPREADSHEET_ID=your-google-sheet-id
GOOGLE_SHEET_NAME=Sheet1

# (Optional) If you use a token file for OAuth2 user flow
# GOOGLE_TOKEN_FILE=path/to/token.json

# Apprise Notification Configuration
# URL for Apprise API endpoint (e.g., http://apprise:8000/notify/your-key)
APPRISE_URL=http://localhost:8000/notify/your-key
# Tag to target specific notification services (e.g., email)
APPRISE_TAG=email
# Path to email template file (optional, uses embedded template if not set)
# EMAIL_TEMPLATE_PATH=/app/templates/email_template.html

# Logging Configuration
# Options: debug, info, warn, error (default: info)
LOG_LEVEL=info